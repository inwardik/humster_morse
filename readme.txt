## Simple script for guessing morse code in Hamster Combat

On the phone:
Go to the device settings, enable adb via wifi. Press 'pair device'.

On PC:
Install adb.

'adb pair addr:port'
input pairing code

'adb connect addr:port'

Check connection by 'adb devices'